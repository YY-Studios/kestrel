# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# env (예시는 추적, 실제 값은 제외)
.env
.env.*
!.env.example

# OS / 에디터
.DS_Store
.idea/
.vscode/

# 개인 로컬 Claude 설정
.claude/settings.local.json

# KIS 접근 토큰 캐시 (키와 동일한 보안 수준 — 절대 커밋 금지)
.kis_token_cache.json
*.kis_token_cache.json

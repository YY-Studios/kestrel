[project]
name = "broker-client"
version = "0.1.0"
description = "증권사 broker-client 추상 계층 (api·engine 공용) — MVP 구현체는 한국투자(KIS) 해외주식"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
